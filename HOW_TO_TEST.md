# 🖼️ Швидке тестування на зображенні

## 📝 Інструкція використання

### Крок 1: Витягніть кадр з відео
```bash
# Витягнути 100-й кадр з відео
ffmpeg -i input/drone_auto.mp4 -vf 'select=eq(n\,100)' -vframes 1 input/test_frame.jpg

# Або 50-й кадр
ffmpeg -i input/drone_auto.mp4 -vf 'select=eq(n\,50)' -vframes 1 input/test_frame.jpg
```

### Крок 2: Відкрийте `test_image.py` і налаштуйте параметри

**Найважливіші параметри (вгорі файлу):**

```python
# --- DEBLURRING ---
DEBLUR_METHOD = "none"     # ← Змініть на "wiener" якщо є blur
WIENER_K = 0.01            # ← Якщо багато шуму - збільште до 0.05

# --- PSF ---
MOTION_LENGTH = 5          # ← ГОЛОВНЕ! Спробуйте 3, 5, 7, 10
MOTION_ANGLE = 0           # ← ВАЖЛИВО! 0, 45, 90, 135
```

### Крок 3: Запустіть
```bash
python test_image.py
```

### Крок 4: Подивіться результат
- `output/test_result.jpg` - результат
- `output/test_result_comparison.jpg` - оригінал і результат поруч

### Крок 5: Якщо результат ОК
Скопіюйте параметри з `test_image.py` в `config.py`:

```python
# test_image.py → config.py
DEBLUR_METHOD = "wiener"    # Скопіюйте
MOTION_LENGTH = 5           # Скопіюйте
MOTION_ANGLE = 0            # Скопіюйте
WIENER_K = 0.01             # Скопіюйте
# ... і т.д.
```

Потім запустіть обробку відео:
```bash
python main_gpu.py
```

---

## 🎯 Що міняти для різних випадків

### Випадок 1: Відео просто темне
```python
DENOISE_METHOD = "none"
DEBLUR_METHOD = "none"
APPLY_CLAHE = True
CLAHE_CLIP_LIMIT = 2.5
```

### Випадок 2: Є горизонтальний blur (лінії розмиті →)
```python
DEBLUR_METHOD = "wiener"
MOTION_ANGLE = 0           # Горизонтально
MOTION_LENGTH = 5          # Почніть з 5
WIENER_K = 0.01
```

### Випадок 3: Вертикальний blur (лінії розмиті ↓)
```python
DEBLUR_METHOD = "wiener"
MOTION_ANGLE = 90          # Вертикально
MOTION_LENGTH = 5
WIENER_K = 0.01
```

### Випадок 4: Діагональний blur (↗ або ↘)
```python
DEBLUR_METHOD = "wiener"
MOTION_ANGLE = 45          # або 135
MOTION_LENGTH = 5
WIENER_K = 0.01
```

### Випадок 5: Багато шуму після deblur
```python
WIENER_K = 0.05            # Збільште!
# або
DENOISE_METHOD = "gaussian"
GAUSSIAN_SIGMA = 0.3
```

---

## 🔄 Процес підбору параметрів

```
1. DEBLUR_METHOD = "none" → запустити → подивитись
   ↓ Якщо blur є
   
2. DEBLUR_METHOD = "wiener" → запустити → подивитись
   ↓ Якщо не допомагає
   
3. Змінити MOTION_ANGLE (0, 45, 90, 135) → тестувати кожен
   ↓ Вибрати найкращий
   
4. Змінити MOTION_LENGTH (3, 5, 7, 10) → тестувати кожен
   ↓ Вибрати найкращий
   
5. Якщо шумно - збільшити WIENER_K (0.01 → 0.02 → 0.05)
   
6. Готово! → скопіювати в config.py
```

---

## ⚙️ Всі параметри в test_image.py

### Основні (які треба міняти):
- `INPUT_IMAGE` - шлях до тестового зображення
- `DEBLUR_METHOD` - метод деблюру ("none", "wiener", "richardson_lucy")
- `MOTION_LENGTH` - довжина blur (3-10)
- `MOTION_ANGLE` - кут blur (0, 45, 90, 135)
- `WIENER_K` - контроль шуму (0.01-0.1)

### Додаткові:
- `DENOISE_METHOD` - метод denoise ("none", "gaussian", "bilateral")
- `APPLY_CLAHE` - контраст (True/False)
- `APPLY_SHARPENING` - різкість (True/False)

---

## ⚡ Швидкі команди

```bash
# Витягнути кадр
ffmpeg -i input/drone_auto.mp4 -vf 'select=eq(n\,100)' -vframes 1 input/test_frame.jpg

# Тестувати
python test_image.py

# Подивитись результат (Linux)
xdg-open output/test_result_comparison.jpg

# Запустити на відео коли готово
python main_gpu.py
```

---

## ❗ Важливо

1. **Завжди дивіться на comparison.jpg** - там оригінал і результат поруч
2. **Якщо результат гірший - відкотіть зміни**
3. **Тестуйте на зображенні ПЕРЕД відео** - економить час
4. **Не використовуйте deblur + sharpening одночасно агресивно**

---

## 📊 Як зрозуміти що параметри правильні

✅ **Добре:**
- Зображення чіткіше
- Кольори природні
- Деталі краще видно
- Немає артефактів

❌ **Погано:**
- З'явився шум
- Розмилось ще більше
- Дивні кольори
- "Ореоли" навколо об'єктів

**Якщо погано → поверніться до DEBLUR_METHOD = "none"**

